{
  "entities": {
    "Vehicle": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Vehicle",
      "type": "object",
      "description": "Represents a vehicle available for booking in the transportation company's fleet.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the vehicle, typically a slug."
        },
        "name": {
          "type": "string",
          "description": "The model name of the vehicle (e.g., LiXiang L7)."
        },
        "imageUrls": {
          "type": "array",
          "description": "URLs of the vehicle's images. The first image is the primary one.",
          "items": {
            "type": "string",
            "format": "uri"
          }
        },
        "category": {
            "type": "string",
            "description": "The category of the vehicle.",
            "enum": ["premium", "comfort", "minivan", "bus"]
        },
        "featureKeys": {
          "type": "array",
          "description": "A list of i18n keys for features offered by the vehicle.",
          "items": {
            "type": "string",
            "enum": ["meet_and_greet", "air_conditioner", "panoramic_view", "ottoman", "tinted_windows", "city_tours"]
          }
        },
        "price": {
          "type": "number",
          "description": "The base price per day for the vehicle, in USD. If 0 or omitted, the price is considered negotiable."
        },
        "capacity": {
          "type": "number",
          "description": "The maximum passenger capacity of the vehicle."
        },
        "isFeatured": {
          "type": "boolean",
          "description": "Whether the vehicle is a featured 'hit' to be shown on the homepage.",
          "default": false
        },
        "displayOrder": {
          "type": "number",
          "description": "Determines the display order of the vehicle on the website. Lower numbers appear first.",
          "default": 0
        }
      },
      "required": [ "id", "name", "imageUrls", "category", "capacity" ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile",
      "type": "object",
      "description": "Represents a user's public profile and roles.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID, matching their Firebase Auth UID."
        },
        "email": {
            "type": "string",
            "format": "email",
            "description": "The user's email address."
        },
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "phone": {
          "type": "string",
          "description": "The user's phone number."
        },
        "isAdmin": {
          "type": "boolean",
          "description": "Whether the user has administrative privileges.",
          "default": false
        }
      },
      "required": ["uid", "email", "isAdmin"]
    },
    "Transfer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transfer",
      "type": "object",
      "description": "Represents a transfer route with pricing information for different vehicle types.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the transfer."
        },
        "serviceType": {
          "type": "string",
          "description": "The type of service provided.",
          "enum": ["intercity", "meet_and_greet", "excursion"]
        },
        "city": {
          "type": "string",
          "description": "The city for 'meet_and_greet' or 'excursion' services."
        },
        "from": {
          "type": "string",
          "description": "The starting point for 'intercity' transfers."
        },
        "to": {
          "type": "string",
          "description": "The destination for 'intercity' transfers."
        },
        "title_ru": {
          "type": "string",
          "description": "Title in Russian, can be auto-generated or custom."
        },
        "title_en": {
          "type": "string",
          "description": "Title in English, can be auto-generated or custom."
        },
        "description_ru": {
          "type": "string",
          "description": "Optional description in Russian."
        },
        "description_en": {
          "type": "string",
          "description": "Optional description in English."
        },
        "prices": {
          "type": "array",
          "description": "A list of prices for different vehicle categories on this route.",
          "items": {
            "type": "object",
            "properties": {
              "category": {
                "type": "string",
                "description": "The category of vehicle this price applies to.",
                "enum": ["premium", "comfort", "minivan", "bus"]
              },
              "price": {
                "type": "number",
                "description": "The price for this vehicle category."
              },
              "vehicleIds": {
                "type": "array",
                "description": "A list of specific vehicle IDs available for this price category on this route.",
                "items": {
                  "type": "string"
                }
              }
            },
            "required": ["category", "price"]
          }
        },
        "isFeatured": {
          "type": "boolean",
          "description": "Whether the transfer is featured on the homepage.",
          "default": false
        },
        "displayOrder": {
          "type": "number",
          "description": "Determines the display order of the transfer on the website. Lower numbers appear first.",
          "default": 0
        }
      },
      "required": ["id", "serviceType", "prices", "title_ru", "title_en"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/vehicles/{vehicleId}",
        "definition": {
          "entityName": "Vehicle",
          "schema": {
            "$ref": "#/backend/entities/Vehicle"
          },
          "description": "Stores vehicle information. Writable only by admins.",
          "params": [
            {
              "name": "vehicleId",
              "description": "The unique identifier of the vehicle."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information and roles. The document ID must match the user's auth UID.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/transfers/{transferId}",
        "definition": {
          "entityName": "Transfer",
          "schema": {
            "$ref": "#/backend/entities/Transfer"
          },
          "description": "Stores transfer route information. Writable only by admins.",
          "params": [
            {
              "name": "transferId",
              "description": "The unique identifier of the transfer."
            }
          ]
        }
      }
    ]
  }
}
